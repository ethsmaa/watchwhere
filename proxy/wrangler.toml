name = "watchwhere-proxy"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# bind KV namespace — id replaced after `wrangler kv namespace create CACHE`
[[kv_namespaces]]
binding = "CACHE"
id = "REPLACE_ME"

# TMDB_TOKEN set via: wrangler secret put TMDB_TOKEN
